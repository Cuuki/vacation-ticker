import LayoutStyle from '@styles/block/layout';
import VisibilityStyle from '@styles/utility/visibility';

const GlobalStyle = () => (
  <>
    <LayoutStyle />
    <VisibilityStyle />
  </>
);

export default GlobalStyle;
