import React from 'react';
import Button from '@material-ui/core/Button';

export default class extends React.Component {
    render() {
        return (
            <div>
                <h1>Vacation Ticker</h1>
                <Button variant="contained" color="primary">
                    Hello World
                </Button>
            </div>
        );
    }
}
